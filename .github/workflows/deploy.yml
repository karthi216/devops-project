
on:
  push:
    branches: [main]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: 📦 Checkout Code
      uses: actions/checkout@v3

    - name: 🐳 Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: 🔐 Login to GHCR
      uses: docker/login-action@v2
      with:
        registry: ghcr.io
        username: karthi216
        password: ${{ secrets.GHCR_PAT }}

    - name: 🛠️ Build Docker Image
      run: docker build -t ghcr.io/karthi216/myapp:latest .

    - name: 🚀 Push Docker Image
      run: docker push ghcr.io/karthi216/myapp:latest
<<<<<<< HEAD

=======
>>>>>>> 442b76c (Polished deployment and updated README)
